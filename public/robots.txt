# SteamHead — https://steamhead.space
#
# Cloudflare's "Managed robots.txt" (zone setting) PREPENDS its content-signal
# and AI-crawler block ABOVE this file — verified live 2026-09-13. So what you
# fetch at /robots.txt is Cloudflare's block first, then this one. Keep this
# file to what Cloudflare does not provide: pointers to our machine-readable
# indexes. Do not restate the Content-Signal or AI-crawler lines here — those
# are managed in the Cloudflare dashboard, and a second copy would drift.
#
# The duplicate "User-agent: *" group this creates is fine: per RFC 9309
# crawlers merge groups that share a user-agent, so the managed Allow: / and
# our Disallow: /admin/ combine. "Sitemap:" is a non-group directive and
# applies wherever it appears.

User-agent: *
Allow: /

# The CMS admin UI is an app, not content. Nothing to index.
Disallow: /admin/

# Discovery. Cloudflare's managed block carries no Sitemap: line, which is the
# only reason this file exists — without it the sitemap is unreachable to any
# crawler that was not told about it by hand.
Sitemap: https://steamhead.space/sitemap-index.xml

# Blog feed, for RSS-to-email and automation (not a crawler directive; here so
# it is discoverable alongside the sitemap).
# Feed: https://steamhead.space/rss.xml
