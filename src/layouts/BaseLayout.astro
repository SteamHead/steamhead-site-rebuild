---
/*
  BaseLayout.astro — shared HTML shell for all pages.
  Props:
    title       (string) — page <title>
    description (string) — meta description
    currentPath (string) — used to mark active nav link
*/
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import '../styles/global.css';

interface Props {
  title: string;
  description?: string;
  currentPath?: string;
}

const {
  title,
  description = "SteamHead is a nonprofit maker education organisation delivering hands-on, project-based STEAM programs.",
  currentPath = Astro.url.pathname,
} = Astro.props;

// Organization structured data — legal name is "SteamHead" (no "LLC"),
// per the 2026-06-22 founders' resolution of the legal-entity question.
// EIN sourced from the /form-990-n/ transparency page.
const orgSchema = {
  "@context": "https://schema.org",
  "@type": "Organization",
  name: "SteamHead",
  legalName: "SteamHead",
  url: "https://steamhead.space",
  logo: "https://steamhead.space/images/branding/sh-logo-horizontal.png",
  description: "SteamHead is a 501(c)(3) nonprofit maker education organisation, founded in 2011, delivering hands-on, project-based STEAM programs for students and educators.",
  foundingDate: "2011",
  taxID: "82-4533355",
  // sameAs tells search engines these profiles are the same organisation as
  // this site — it is how a channel or group gets associated with SteamHead
  // rather than treated as an unrelated account.
  sameAs: [
    "https://www.facebook.com/groups/steamhead.space",
    "https://www.youtube.com/channel/UCrw4Xp93J8I4xXdldmW8vJg",
    "https://www.instagram.com/makefashionedu/",
  ],
  // The licence that covers this site's writing and curriculum. Photographs of
  // identifiable people are reserved and are NOT covered — structured data has
  // no way to express a per-asset split, so /license/ states it in words.
  license: "https://creativecommons.org/licenses/by-sa/4.0/",
  contactPoint: {
    "@type": "ContactPoint",
    email: "james@steamhead.space",
    contactType: "general inquiries",
  },
};
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="generator" content={Astro.generator} />

    <title>{title} — SteamHead</title>
    <meta name="description" content={description} />

    <!-- Open Graph -->
    <meta property="og:title" content={`${title} — SteamHead`} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />

    <link rel="icon" type="image/png" href="/favicon.png" />

    <!-- Feed auto-discovery: how readers and RSS-to-email tools find /rss.xml
         when someone hands them just "steamhead.space". -->
    <link
      rel="alternate"
      type="application/rss+xml"
      title="SteamHead — Great Things Blog"
      href="/rss.xml"
    />

    <script type="application/ld+json" set:html={JSON.stringify(orgSchema)} />
  </head>
  <body>

    <a href="#main-content" class="skip-link">Skip to main content</a>

    <Header currentPath={currentPath} />

    <main id="main-content">
      <slot />
    </main>

    <Footer />

  </body>
</html>
